<!--
  Torque Loop's rule: no proof → no keep. Show the proof.
  Keep the diff minimal: REMOVE / ADD / CHANGE only what the change requires.
-->

## What this changes

<!-- One or two sentences. What did you change and why? -->

## What failed before

<!-- The defect, gap, or friction this addresses. Link the issue: Closes #___ -->

## What proves it works now

<!-- Tests added/updated, commands run, output. "It should work" is not proof. -->

```
# paste the relevant `npm test` output or command run here
```

## Checklist

- [ ] `npm test` passes locally
- [ ] Added/updated a test that would fail without this change (or explained why none applies)
- [ ] No new runtime dependencies (or justified in the description)
- [ ] Docs updated if behavior or commands changed (`README.md` / `reference/PROMPTS.md`)
- [ ] Diff is minimal — no unrelated changes
